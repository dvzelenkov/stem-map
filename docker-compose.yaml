services:
  database:
    image: postgres
    ports:
      - ${DB_CORE_PORT}:5432
    environment:
      POSTGRES_PASSWORD: dev
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
    name: postgres-data