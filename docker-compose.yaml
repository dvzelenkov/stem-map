services:
  database:
    image: postgres
    ports:
      - ${DB_CORE_PORT}:5432
    environment:
      POSTGRES_PASSWORD: dev
    volumes:
      - study-postgres-data:/var/lib/postgresql/data
volumes:
  study-postgres-data:
    name: study-postgres-data